# Required for AWS Amplify compatibility with pnpm workspaces.
# Amplify's build environment expects a flat node_modules layout.
node-linker=hoisted

# Prevent pnpm from failing on peer dependency warnings
strict-peer-dependencies=false

# Allow workspace protocol in dependencies
link-workspace-packages=true
